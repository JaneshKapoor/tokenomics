{
  "name": "tokenomics-mcp",
  "version": "0.1.0",
  "description": "Tokenomics — MCP server exposing AI cost/token usage data (spend by team/agent, top expensive requests, model mix, anomaly flags) as structured tools.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "tokenomics-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test:tools": "tsc && node dist/test-harness.js",
    "smoke": "tsc && node scripts/mcp-smoke.mjs",
    "smoke:docker": "node scripts/mcp-smoke.mjs docker run -i --rm -e TOKENOMICS_DATA_SOURCE=synthetic tokenomics-mcp",
    "docker:build": "docker build -t tokenomics-mcp ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
